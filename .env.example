# Docker publishes only to this host interface by default.
AGENT_SPEAK_PUBLISH_HOST=127.0.0.1
AGENT_SPEAK_PORT=8765
# Optional container identity/audio-group overrides. ./run.sh detects them automatically.
# AGENT_SPEAK_UID=1000
# AGENT_SPEAK_GID=1000
# AGENT_SPEAK_AUDIO_GID=29
# stdio MCP connects to the Gateway inside the running container.
AGENT_SPEAK_URL=http://127.0.0.1:8765

# Private HTTPS access through Tailscale Serve; the app remains localhost-only.
AGENT_SPEAK_TAILSCALE_HTTPS_PORT=8765

# Host bind paths for private persistent state. Relative paths are resolved by Compose.
AGENT_SPEAK_DATA_PATH=./data
AGENT_SPEAK_RUNTIME_PATH=./runtime
AGENT_SPEAK_MODELS_PATH=./models

# WAV requests are rejected before decoding when these limits are exceeded.
AGENT_SPEAK_MAX_AUDIO_BYTES=8388608
AGENT_SPEAK_MAX_AUDIO_SECONDS=30
AGENT_SPEAK_VAD_RMS_THRESHOLD=0.015

# Bounded in-memory and generated-artifact retention for the MVP.
AGENT_SPEAK_MAX_SESSIONS=100
AGENT_SPEAK_MAX_SESSION_EVENTS=128
AGENT_SPEAK_MAX_EVENT_QUEUE=64
AGENT_SPEAK_MAX_ARTIFACTS=100

# Local production speech providers. Faster-Whisper loads lazily on first turn.
# auto uses NVIDIA only when both the driver and Docker runtime are ready.
# cpu forces the portable image; nvidia fails rather than silently falling back.
AGENT_SPEAK_ACCELERATOR=auto
AGENT_SPEAK_ASR_MODEL=small
AGENT_SPEAK_ASR_LANGUAGE=zh
AGENT_SPEAK_ASR_COMPUTE_TYPE=int8
AGENT_SPEAK_ASR_CUDA_COMPUTE_TYPE=float16
AGENT_SPEAK_ASR_CPU_THREADS=4
AGENT_SPEAK_TTS_MODEL_PATH=models/piper/zh_CN-huayan-medium.onnx

# Realtime browser stream contract and bounded pipeline queues.
AGENT_SPEAK_REALTIME_FRAME_MS=20
AGENT_SPEAK_REALTIME_PRE_ROLL_MS=300
AGENT_SPEAK_REALTIME_MIN_SPEECH_MS=250
AGENT_SPEAK_REALTIME_PARTIAL_INTERVAL_MS=800
AGENT_SPEAK_REALTIME_ENDPOINT_MS=900
AGENT_SPEAK_REALTIME_HARD_ENDPOINT_MS=1800
AGENT_SPEAK_REALTIME_ENDPOINT_TIMEOUT_MS=250
AGENT_SPEAK_REALTIME_MAX_UTTERANCE_SECONDS=30
AGENT_SPEAK_REALTIME_PARTIAL_QUEUE=8
AGENT_SPEAK_REALTIME_FINAL_QUEUE=8
AGENT_SPEAK_REALTIME_TEXT_QUEUE=8
AGENT_SPEAK_REALTIME_EXPECTED_DEVICE=Zone Vibe 100

# Internal workers are optional until the realtime topology is enabled.
AGENT_SPEAK_ASR_WORKER_URL=
AGENT_SPEAK_CORRECTION_WORKER_URL=
AGENT_SPEAK_CORRECTION_MODEL=Qwen2.5-1.5B-Instruct-Q4_K_M
AGENT_SPEAK_EFFECTIVE_ACCELERATOR=cpu
